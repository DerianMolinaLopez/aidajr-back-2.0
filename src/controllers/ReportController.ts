import { Request, Response } from 'express';
class Reportes{
    //1-metodo para sacar el total de grupos por maestro
    //desde el front viene un tokend e autenticacion
    //asi que solo es necesario ver que hay en req.user para buscar el usuario
    /**
         * Calcula el porcentaje de finalización de un curso.
         *
         * @param {Token}  id, email - entrada procesada por req.user.
         * @returns {number} - Numero de alumnos inscritos
       */

      /** 
         * 2- Total de alumnos por instructor
         *
         * @param {Token}  id, email - entrada procesada por req.user.
         * @returns {number} - Numero de alumnos inscritos
       */

      /** 
         * 3- Tareas pendientes o no revisada
         *
         * @param {Token} id, email - entrada procesada por req.user.
         * @returns {number} - Numero de alumnos inscritos
       */

      /** 
         * 4- Total de codigos de union incritos por el instructor
         *
         * @param {Token} id, email - entrada procesada por req.user.
         * @returns {number} - Numero de alumnos inscritos
       */

}